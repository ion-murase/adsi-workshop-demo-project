spring:
  docker:
    compose:
      lifecycle-management: start-and-stop

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.example.attendance: DEBUG
    org.springframework.security: DEBUG
